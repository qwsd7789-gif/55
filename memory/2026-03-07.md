# 2026-03-07

- 用户反馈：在“视频流 agent”中链接打不开时，未按既定规则优先尝试 `r.jina.ai` 前缀，而是直接给其他选项。
- 已执行加固：更新 `AGENTS.md` 两处 Link Fetch Fallback 规则为强制自动重试（MUST / ALWAYS），并明确“失败后才给替代方案”。
- 规则细化：重试前不询问；不得跳过该步骤；若失败需说明已重试。
- 新增经验：小红书链接打不开时，按“r.jina.ai 前缀失败→找可抓取 agent/浏览器自动化兜底→最后才给人工方案”执行。
