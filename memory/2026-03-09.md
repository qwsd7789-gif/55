# 2026-03-09

- 用户新增明确偏好：今后凡是用户询问任何 skill，优先使用 `find-skills` 技能进行检索。
- 输出要求：在推荐/介绍 skill 时，必须优先告诉用户该 skill 的安装量与具体功能，再给建议或后续动作。
- 用户新增默认安装/配置策略：以后安装 skill，若未特别说明，默认全局安装并尽量让所有 agent 可见/可用；涉及 API/key/cookie/token 等配置时，也优先落到其他 agent 可复用的全局配置位置。
- 用户新增全局执行偏好：以后找数据、打开网站、使用功能时，先优先检查和使用已安装的 skill；只有没有合适 skill 时，才去外部源。
- 该偏好需要作为长期工作规则执行。
- 用户补充了全局规则：这些偏好也应写入全局 `AGENTS.md`，作为所有 agent 的共用执行规则。
- 已安装并验证浏览器自动化 skill：`oakoss/agent-skills@playwright`（安装量 69）。本机已成功安装 Playwright Chromium，并已实际打开网页与生成截图，说明浏览器自动化链路可用。
- 用户确认可优先使用这套 Playwright Chromium 作为后续网页自动化浏览器；如长期使用，适合后续固化为固定浏览器/profile 工作流。
- X/Twitter CLI 路线已验证可用：`agent-reach` / `xreach` 已安装，使用用户提供的 `auth_token + ct0` 可成功执行搜索。用户明确要求：这套 X 凭据**不要全局共享**，仅限 `x-chuangzuo` 与 `video-stream` 两个 agent 使用。
- 已为 `x-chuangzuo` 与 `video-stream` 各自创建本地 X helper 脚本：各自 agent 目录下有 `xreach-auth.ps1` / `xreach-search.ps1`，用于仅该 agent 使用的 X 调用路径。
- Suxi API 路线已重新确认并跑通：
  - 入口脚本：`workspace/suxi_image.mjs`
  - 读取配置：`SUXI_API_KEY` 或 `OPENAI_API_KEY`
  - Base URL：`OPENAI_BASE_URL=https://new.suxi.ai/v1`
  - 默认模型：`gemini-2.5-flash-image`
- 已验证：Suxi 走 OpenAI-compatible API 可直接完成“参考图 + 提示词”的图片重绘，不必优先走网页自动化。
- 为批量小红书图片重绘新增并验证脚本：
  - `scripts/suxi_edit_run.mjs`：单张参考图重绘
  - `scripts/suxi_batch_xhs.mjs`：第 3/4 篇批量重绘并产出 HTML 对照页
  - `scripts/suxi_batch_xhs_7_10.mjs`：第 7~10 篇每篇前 2 张批量重绘并产出 HTML 对照页
- 已产出可查看结果：
  - 单张结果：`outputs/suxi-edit-1773043874672.png`
  - 第 3/4 篇批量结果：`outputs/suxi-batch-1773044383444/result.html`
  - 第 7~10 篇批量结果：`outputs/suxi-batch-7to10-1773044783698/result.html`
- 在第 3/4 篇批处理中，第 4 篇的第 3、4 张图失败；`manifest.json` 记录原因为 `fetch failed`，判断更像请求链路/连接波动，非参数错误，重试大概率可过。
