
## 13:04 视频文案技能全局策略落地（video-copy-analyzer）
- 用户明确要求：将 `video-copy-analyzer` 同步给所有 agent，作为共享全局 skill 使用。
- 用户新增强规则：当链接/文件中有视频，且需求是“提取文案、提取字幕、获取口播内容、理解视频内容”时，默认自动使用 `video-copy-analyzer`。
- 适用平台明确包括：小红书、抖音、B站、YouTube 等视频链接。
- 已完成执行：
  - 修复了技能阶段2依赖问题（安装 ffmpeg 后，FunASR 转录成功，生成 SRT）。
  - 已将规则写入全局规则文件（AGENTS.md / MEMORY.md / TOOLS.md）。
  - 已提交 git 记录：`d24e771`（Add global default rule for video-copy-analyzer）。
- 用户当前偏好确认：以后视频提取/理解类请求，不需要反复确认技能选择，按该默认规则执行。
