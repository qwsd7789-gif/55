# 2026-03-18
- 用户要求清空 OpenClaw agent fallback，仅使用当前主模型；已将 `C:\Users\Administrator\.openclaw\openclaw.json` 中 `agents.defaults.model.fallbacks` 设为 `[]`，并重启 gateway 生效。
- 用户要求将全局 agent 超时调高；已将 `agents.defaults.timeoutSeconds` 设为 `180`。
- 用户要求主会话默认回测引擎使用 `vectorbt`；已写入主工作区 `AGENTS.md`：未特别指定时默认用 vectorbt。
- 用户要求所有回测中的“年化”统一按复合年化收益率（CAGR）口径；已写入主工作区 `AGENTS.md`。
- 用户澄清：主会话 agent（不是 agent2-polymarket）做回测时默认用 vectorbt。
- 为 `workspace-agent2-polymarket` 写入大文件分块处理规则；同时为该 agent 也写入默认回测引擎用 vectorbt 的规则。
- 今日进行了多组 vectorbt 回测，用户重点关注美股/QQ/SPY 动量策略、止损、PIT 成分、双开关与 MA200 过滤。
- 用户明确要求 QQQ Top5% 回测要尽量使用当期成分（PIT rolling constituents）以避免未来函数；后续相关 QQQ/NDX 回测应优先使用 PIT 成分口径。
- 已完成并保存的重要回测结果文件包括：
  - `tmp/qq_12_1_gate_2016_now_vectorbt.csv`
  - `tmp/sp500_mom12_1_top5_2016_now_vectorbt.csv`
  - `tmp/sp500_mom12_1_top20_2016_now_vectorbt.csv`
  - `tmp/qq_gate_top_sweep_2016_now_vectorbt.csv`
  - `tmp/qq_gate_top_sweep_2016_now_vectorbt_retry.csv`
  - `tmp/spy_gate_top_sweep_2016_now_vectorbt.csv`
  - `tmp/qq_pit_top5_2016_now_vectorbt.csv`
  - `tmp/qq_pit_top5_checkpoints.csv`
  - `tmp/qq_pit_top5_gate_ma200_2016_now_vectorbt.csv`
  - `tmp/qq_pit_top5_single_vs_dual_gate_2016_now_vectorbt.csv`
  - `tmp/qq_pit_top5_gate_spy12_1_2016_now_vectorbt.csv`
  - `tmp/momentum_stoploss_e_backtest_summary.csv`
  - `tmp/momentum_stoploss_e_checkpoints.csv`
  - `tmp/momentum_stoploss_e_single12_summary.csv`
- 今日经验：同一策略多次回测结果不一致的主要漂移源来自 yfinance 对部分历史/退市 ticker 的拉取失败；若要稳定复现，需锁定数据快照或采用更稳定的历史数据源。
- 用户关注的策略优化方向：降低 QQQ 12-1 开关策略回撤，已测试双开关（QQ 12-1 > 0 且 QQQ > MA200），结果显示双开关明显降低 MDD。
