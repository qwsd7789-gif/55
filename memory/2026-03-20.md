# 2026-03-20
## Skill 变更
- 用户要求删除 `aibtc` skill。
- 已删除目录：`skills/aibtc`

## Reddit 抓取偏好
- 用户将 Reddit 抓取方向改为：财经、股票、伊朗。
- 已同步更新 `scripts/reddit_subs_watch.ps1` 的分类与 subreddit 列表。

## 6551 快报
- 用户要求停掉这边的 6551 快报。
- 已将 `config/breaking-news-cron.yaml` 中的任务设为 `enabled: false`。

## ClawChain 挖矿
- 用户要求开始 ClawChain 挖矿。
- 已创建 `skills/clawchain-miner` 并写入 `setup.py` / `mine.py` / `status.py` / `config.json`。
- 已生成并注册矿工钱包：`claw18qya8c44t0yjv37qgrxjhpy396nly3fvw95nw0`。
- 已成功跑通首轮挖矿，完成 1 道数学题并提交成功。
- 用户随后要求“持续挖”，已创建 OpenClaw cron 任务：每 10 分钟自动执行一轮挖矿，默认静默运行，仅在阻塞/持续失败时提示。
- 用户要求把挖矿脚本接到当前 OpenClaw 的 codexmanager。已修改 `skills/clawchain-miner/scripts/mine.py`，支持从 `~/.openclaw/openclaw.json` 自动读取 codexmanager 的 OpenAI 兼容 `baseUrl/apiKey/model`。
- 当前联调受链节点影响：`https://russell-pennsylvania-joke-avatar.trycloudflare.com` 返回 530，暂时无法完成端到端验证。
