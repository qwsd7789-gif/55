# 2026-03-24
- 用户提供 GitHub 仓库 `jackwener/opencli`，要求安装；已通过 `npm install -g @jackwener/opencli` 完成全局安装，版本 `1.3.3`。
- 用户新增偏好：以后在对话中如需打开网页、读取网页内容、抓取页面数据，默认优先使用 `opencli`；仅在 `opencli` 不适合、不可用或目标不支持时，才退回其他抓取方案。
